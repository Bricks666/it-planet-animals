// Code generated by ent, DO NOT EDIT.

package ent

import (
	"animals/ent/animal"
	"animals/ent/animalslocations"
	"animals/ent/location"
	"animals/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AnimalsLocationsQuery is the builder for querying AnimalsLocations entities.
type AnimalsLocationsQuery struct {
	config
	ctx                          *QueryContext
	order                        []OrderFunc
	inters                       []Interceptor
	predicates                   []predicate.AnimalsLocations
	withAnimalsLocationsAnimal   *AnimalQuery
	withAnimalsLocationsLocation *LocationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AnimalsLocationsQuery builder.
func (alq *AnimalsLocationsQuery) Where(ps ...predicate.AnimalsLocations) *AnimalsLocationsQuery {
	alq.predicates = append(alq.predicates, ps...)
	return alq
}

// Limit the number of records to be returned by this query.
func (alq *AnimalsLocationsQuery) Limit(limit int) *AnimalsLocationsQuery {
	alq.ctx.Limit = &limit
	return alq
}

// Offset to start from.
func (alq *AnimalsLocationsQuery) Offset(offset int) *AnimalsLocationsQuery {
	alq.ctx.Offset = &offset
	return alq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (alq *AnimalsLocationsQuery) Unique(unique bool) *AnimalsLocationsQuery {
	alq.ctx.Unique = &unique
	return alq
}

// Order specifies how the records should be ordered.
func (alq *AnimalsLocationsQuery) Order(o ...OrderFunc) *AnimalsLocationsQuery {
	alq.order = append(alq.order, o...)
	return alq
}

// QueryAnimalsLocationsAnimal chains the current query on the "animals_locations_animal" edge.
func (alq *AnimalsLocationsQuery) QueryAnimalsLocationsAnimal() *AnimalQuery {
	query := (&AnimalClient{config: alq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := alq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := alq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(animalslocations.Table, animalslocations.FieldID, selector),
			sqlgraph.To(animal.Table, animal.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, animalslocations.AnimalsLocationsAnimalTable, animalslocations.AnimalsLocationsAnimalColumn),
		)
		fromU = sqlgraph.SetNeighbors(alq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAnimalsLocationsLocation chains the current query on the "animals_locations_location" edge.
func (alq *AnimalsLocationsQuery) QueryAnimalsLocationsLocation() *LocationQuery {
	query := (&LocationClient{config: alq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := alq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := alq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(animalslocations.Table, animalslocations.FieldID, selector),
			sqlgraph.To(location.Table, location.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, animalslocations.AnimalsLocationsLocationTable, animalslocations.AnimalsLocationsLocationColumn),
		)
		fromU = sqlgraph.SetNeighbors(alq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AnimalsLocations entity from the query.
// Returns a *NotFoundError when no AnimalsLocations was found.
func (alq *AnimalsLocationsQuery) First(ctx context.Context) (*AnimalsLocations, error) {
	nodes, err := alq.Limit(1).All(setContextOp(ctx, alq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{animalslocations.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) FirstX(ctx context.Context) *AnimalsLocations {
	node, err := alq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AnimalsLocations ID from the query.
// Returns a *NotFoundError when no AnimalsLocations ID was found.
func (alq *AnimalsLocationsQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = alq.Limit(1).IDs(setContextOp(ctx, alq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{animalslocations.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := alq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AnimalsLocations entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AnimalsLocations entity is found.
// Returns a *NotFoundError when no AnimalsLocations entities are found.
func (alq *AnimalsLocationsQuery) Only(ctx context.Context) (*AnimalsLocations, error) {
	nodes, err := alq.Limit(2).All(setContextOp(ctx, alq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{animalslocations.Label}
	default:
		return nil, &NotSingularError{animalslocations.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) OnlyX(ctx context.Context) *AnimalsLocations {
	node, err := alq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AnimalsLocations ID in the query.
// Returns a *NotSingularError when more than one AnimalsLocations ID is found.
// Returns a *NotFoundError when no entities are found.
func (alq *AnimalsLocationsQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = alq.Limit(2).IDs(setContextOp(ctx, alq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{animalslocations.Label}
	default:
		err = &NotSingularError{animalslocations.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := alq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AnimalsLocationsSlice.
func (alq *AnimalsLocationsQuery) All(ctx context.Context) ([]*AnimalsLocations, error) {
	ctx = setContextOp(ctx, alq.ctx, "All")
	if err := alq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AnimalsLocations, *AnimalsLocationsQuery]()
	return withInterceptors[[]*AnimalsLocations](ctx, alq, qr, alq.inters)
}

// AllX is like All, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) AllX(ctx context.Context) []*AnimalsLocations {
	nodes, err := alq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AnimalsLocations IDs.
func (alq *AnimalsLocationsQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if alq.ctx.Unique == nil && alq.path != nil {
		alq.Unique(true)
	}
	ctx = setContextOp(ctx, alq.ctx, "IDs")
	if err = alq.Select(animalslocations.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := alq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (alq *AnimalsLocationsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, alq.ctx, "Count")
	if err := alq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, alq, querierCount[*AnimalsLocationsQuery](), alq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) CountX(ctx context.Context) int {
	count, err := alq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (alq *AnimalsLocationsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, alq.ctx, "Exist")
	switch _, err := alq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (alq *AnimalsLocationsQuery) ExistX(ctx context.Context) bool {
	exist, err := alq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AnimalsLocationsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (alq *AnimalsLocationsQuery) Clone() *AnimalsLocationsQuery {
	if alq == nil {
		return nil
	}
	return &AnimalsLocationsQuery{
		config:                       alq.config,
		ctx:                          alq.ctx.Clone(),
		order:                        append([]OrderFunc{}, alq.order...),
		inters:                       append([]Interceptor{}, alq.inters...),
		predicates:                   append([]predicate.AnimalsLocations{}, alq.predicates...),
		withAnimalsLocationsAnimal:   alq.withAnimalsLocationsAnimal.Clone(),
		withAnimalsLocationsLocation: alq.withAnimalsLocationsLocation.Clone(),
		// clone intermediate query.
		sql:  alq.sql.Clone(),
		path: alq.path,
	}
}

// WithAnimalsLocationsAnimal tells the query-builder to eager-load the nodes that are connected to
// the "animals_locations_animal" edge. The optional arguments are used to configure the query builder of the edge.
func (alq *AnimalsLocationsQuery) WithAnimalsLocationsAnimal(opts ...func(*AnimalQuery)) *AnimalsLocationsQuery {
	query := (&AnimalClient{config: alq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	alq.withAnimalsLocationsAnimal = query
	return alq
}

// WithAnimalsLocationsLocation tells the query-builder to eager-load the nodes that are connected to
// the "animals_locations_location" edge. The optional arguments are used to configure the query builder of the edge.
func (alq *AnimalsLocationsQuery) WithAnimalsLocationsLocation(opts ...func(*LocationQuery)) *AnimalsLocationsQuery {
	query := (&LocationClient{config: alq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	alq.withAnimalsLocationsLocation = query
	return alq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AnimalId uint64 `json:"animalId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AnimalsLocations.Query().
//		GroupBy(animalslocations.FieldAnimalId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (alq *AnimalsLocationsQuery) GroupBy(field string, fields ...string) *AnimalsLocationsGroupBy {
	alq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AnimalsLocationsGroupBy{build: alq}
	grbuild.flds = &alq.ctx.Fields
	grbuild.label = animalslocations.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AnimalId uint64 `json:"animalId,omitempty"`
//	}
//
//	client.AnimalsLocations.Query().
//		Select(animalslocations.FieldAnimalId).
//		Scan(ctx, &v)
func (alq *AnimalsLocationsQuery) Select(fields ...string) *AnimalsLocationsSelect {
	alq.ctx.Fields = append(alq.ctx.Fields, fields...)
	sbuild := &AnimalsLocationsSelect{AnimalsLocationsQuery: alq}
	sbuild.label = animalslocations.Label
	sbuild.flds, sbuild.scan = &alq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AnimalsLocationsSelect configured with the given aggregations.
func (alq *AnimalsLocationsQuery) Aggregate(fns ...AggregateFunc) *AnimalsLocationsSelect {
	return alq.Select().Aggregate(fns...)
}

func (alq *AnimalsLocationsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range alq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, alq); err != nil {
				return err
			}
		}
	}
	for _, f := range alq.ctx.Fields {
		if !animalslocations.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if alq.path != nil {
		prev, err := alq.path(ctx)
		if err != nil {
			return err
		}
		alq.sql = prev
	}
	return nil
}

func (alq *AnimalsLocationsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AnimalsLocations, error) {
	var (
		nodes       = []*AnimalsLocations{}
		_spec       = alq.querySpec()
		loadedTypes = [2]bool{
			alq.withAnimalsLocationsAnimal != nil,
			alq.withAnimalsLocationsLocation != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AnimalsLocations).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AnimalsLocations{config: alq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, alq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := alq.withAnimalsLocationsAnimal; query != nil {
		if err := alq.loadAnimalsLocationsAnimal(ctx, query, nodes, nil,
			func(n *AnimalsLocations, e *Animal) { n.Edges.AnimalsLocationsAnimal = e }); err != nil {
			return nil, err
		}
	}
	if query := alq.withAnimalsLocationsLocation; query != nil {
		if err := alq.loadAnimalsLocationsLocation(ctx, query, nodes, nil,
			func(n *AnimalsLocations, e *Location) { n.Edges.AnimalsLocationsLocation = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (alq *AnimalsLocationsQuery) loadAnimalsLocationsAnimal(ctx context.Context, query *AnimalQuery, nodes []*AnimalsLocations, init func(*AnimalsLocations), assign func(*AnimalsLocations, *Animal)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*AnimalsLocations)
	for i := range nodes {
		fk := nodes[i].AnimalId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(animal.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "animalId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (alq *AnimalsLocationsQuery) loadAnimalsLocationsLocation(ctx context.Context, query *LocationQuery, nodes []*AnimalsLocations, init func(*AnimalsLocations), assign func(*AnimalsLocations, *Location)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*AnimalsLocations)
	for i := range nodes {
		fk := nodes[i].LocationId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(location.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "locationId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (alq *AnimalsLocationsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := alq.querySpec()
	_spec.Node.Columns = alq.ctx.Fields
	if len(alq.ctx.Fields) > 0 {
		_spec.Unique = alq.ctx.Unique != nil && *alq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, alq.driver, _spec)
}

func (alq *AnimalsLocationsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(animalslocations.Table, animalslocations.Columns, sqlgraph.NewFieldSpec(animalslocations.FieldID, field.TypeUint64))
	_spec.From = alq.sql
	if unique := alq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if alq.path != nil {
		_spec.Unique = true
	}
	if fields := alq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, animalslocations.FieldID)
		for i := range fields {
			if fields[i] != animalslocations.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := alq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := alq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := alq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := alq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (alq *AnimalsLocationsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(alq.driver.Dialect())
	t1 := builder.Table(animalslocations.Table)
	columns := alq.ctx.Fields
	if len(columns) == 0 {
		columns = animalslocations.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if alq.sql != nil {
		selector = alq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if alq.ctx.Unique != nil && *alq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range alq.predicates {
		p(selector)
	}
	for _, p := range alq.order {
		p(selector)
	}
	if offset := alq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := alq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AnimalsLocationsGroupBy is the group-by builder for AnimalsLocations entities.
type AnimalsLocationsGroupBy struct {
	selector
	build *AnimalsLocationsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (algb *AnimalsLocationsGroupBy) Aggregate(fns ...AggregateFunc) *AnimalsLocationsGroupBy {
	algb.fns = append(algb.fns, fns...)
	return algb
}

// Scan applies the selector query and scans the result into the given value.
func (algb *AnimalsLocationsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, algb.build.ctx, "GroupBy")
	if err := algb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnimalsLocationsQuery, *AnimalsLocationsGroupBy](ctx, algb.build, algb, algb.build.inters, v)
}

func (algb *AnimalsLocationsGroupBy) sqlScan(ctx context.Context, root *AnimalsLocationsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(algb.fns))
	for _, fn := range algb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*algb.flds)+len(algb.fns))
		for _, f := range *algb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*algb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := algb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AnimalsLocationsSelect is the builder for selecting fields of AnimalsLocations entities.
type AnimalsLocationsSelect struct {
	*AnimalsLocationsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (als *AnimalsLocationsSelect) Aggregate(fns ...AggregateFunc) *AnimalsLocationsSelect {
	als.fns = append(als.fns, fns...)
	return als
}

// Scan applies the selector query and scans the result into the given value.
func (als *AnimalsLocationsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, als.ctx, "Select")
	if err := als.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnimalsLocationsQuery, *AnimalsLocationsSelect](ctx, als.AnimalsLocationsQuery, als, als.inters, v)
}

func (als *AnimalsLocationsSelect) sqlScan(ctx context.Context, root *AnimalsLocationsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(als.fns))
	for _, fn := range als.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*als.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := als.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
