// Code generated by ent, DO NOT EDIT.

package ent

import (
	"animals/ent/animaltag"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// AnimalTag is the model entity for the AnimalTag schema.
type AnimalTag struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// Type holds the value of the "type" field.
	Type string `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AnimalTagQuery when eager-loading is set.
	Edges AnimalTagEdges `json:"edges"`
}

// AnimalTagEdges holds the relations/edges for other nodes in the graph.
type AnimalTagEdges struct {
	// Animals holds the value of the animals edge.
	Animals []*Animal `json:"animals,omitempty"`
	// AnimalTypes holds the value of the animal_types edge.
	AnimalTypes []*AnimalType `json:"animal_types,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// AnimalsOrErr returns the Animals value or an error if the edge
// was not loaded in eager-loading.
func (e AnimalTagEdges) AnimalsOrErr() ([]*Animal, error) {
	if e.loadedTypes[0] {
		return e.Animals, nil
	}
	return nil, &NotLoadedError{edge: "animals"}
}

// AnimalTypesOrErr returns the AnimalTypes value or an error if the edge
// was not loaded in eager-loading.
func (e AnimalTagEdges) AnimalTypesOrErr() ([]*AnimalType, error) {
	if e.loadedTypes[1] {
		return e.AnimalTypes, nil
	}
	return nil, &NotLoadedError{edge: "animal_types"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AnimalTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case animaltag.FieldID:
			values[i] = new(sql.NullInt64)
		case animaltag.FieldType:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type AnimalTag", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AnimalTag fields.
func (at *AnimalTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case animaltag.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			at.ID = uint64(value.Int64)
		case animaltag.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				at.Type = value.String
			}
		}
	}
	return nil
}

// QueryAnimals queries the "animals" edge of the AnimalTag entity.
func (at *AnimalTag) QueryAnimals() *AnimalQuery {
	return NewAnimalTagClient(at.config).QueryAnimals(at)
}

// QueryAnimalTypes queries the "animal_types" edge of the AnimalTag entity.
func (at *AnimalTag) QueryAnimalTypes() *AnimalTypeQuery {
	return NewAnimalTagClient(at.config).QueryAnimalTypes(at)
}

// Update returns a builder for updating this AnimalTag.
// Note that you need to call AnimalTag.Unwrap() before calling this method if this AnimalTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (at *AnimalTag) Update() *AnimalTagUpdateOne {
	return NewAnimalTagClient(at.config).UpdateOne(at)
}

// Unwrap unwraps the AnimalTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (at *AnimalTag) Unwrap() *AnimalTag {
	_tx, ok := at.config.driver.(*txDriver)
	if !ok {
		panic("ent: AnimalTag is not a transactional entity")
	}
	at.config.driver = _tx.drv
	return at
}

// String implements the fmt.Stringer.
func (at *AnimalTag) String() string {
	var builder strings.Builder
	builder.WriteString("AnimalTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", at.ID))
	builder.WriteString("type=")
	builder.WriteString(at.Type)
	builder.WriteByte(')')
	return builder.String()
}

// AnimalTags is a parsable slice of AnimalTag.
type AnimalTags []*AnimalTag
