// Code generated by ent, DO NOT EDIT.

package ent

import (
	"animals/ent/animal"
	"animals/ent/animaltype"
	"animals/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AnimalTypeQuery is the builder for querying AnimalType entities.
type AnimalTypeQuery struct {
	config
	ctx                *QueryContext
	order              []OrderFunc
	inters             []Interceptor
	predicates         []predicate.AnimalType
	withAnimalTypeType *AnimalQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AnimalTypeQuery builder.
func (atq *AnimalTypeQuery) Where(ps ...predicate.AnimalType) *AnimalTypeQuery {
	atq.predicates = append(atq.predicates, ps...)
	return atq
}

// Limit the number of records to be returned by this query.
func (atq *AnimalTypeQuery) Limit(limit int) *AnimalTypeQuery {
	atq.ctx.Limit = &limit
	return atq
}

// Offset to start from.
func (atq *AnimalTypeQuery) Offset(offset int) *AnimalTypeQuery {
	atq.ctx.Offset = &offset
	return atq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (atq *AnimalTypeQuery) Unique(unique bool) *AnimalTypeQuery {
	atq.ctx.Unique = &unique
	return atq
}

// Order specifies how the records should be ordered.
func (atq *AnimalTypeQuery) Order(o ...OrderFunc) *AnimalTypeQuery {
	atq.order = append(atq.order, o...)
	return atq
}

// QueryAnimalTypeType chains the current query on the "animal_type_type" edge.
func (atq *AnimalTypeQuery) QueryAnimalTypeType() *AnimalQuery {
	query := (&AnimalClient{config: atq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := atq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := atq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(animaltype.Table, animaltype.FieldID, selector),
			sqlgraph.To(animal.Table, animal.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, animaltype.AnimalTypeTypeTable, animaltype.AnimalTypeTypePrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(atq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AnimalType entity from the query.
// Returns a *NotFoundError when no AnimalType was found.
func (atq *AnimalTypeQuery) First(ctx context.Context) (*AnimalType, error) {
	nodes, err := atq.Limit(1).All(setContextOp(ctx, atq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{animaltype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (atq *AnimalTypeQuery) FirstX(ctx context.Context) *AnimalType {
	node, err := atq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AnimalType ID from the query.
// Returns a *NotFoundError when no AnimalType ID was found.
func (atq *AnimalTypeQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = atq.Limit(1).IDs(setContextOp(ctx, atq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{animaltype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (atq *AnimalTypeQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := atq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AnimalType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AnimalType entity is found.
// Returns a *NotFoundError when no AnimalType entities are found.
func (atq *AnimalTypeQuery) Only(ctx context.Context) (*AnimalType, error) {
	nodes, err := atq.Limit(2).All(setContextOp(ctx, atq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{animaltype.Label}
	default:
		return nil, &NotSingularError{animaltype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (atq *AnimalTypeQuery) OnlyX(ctx context.Context) *AnimalType {
	node, err := atq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AnimalType ID in the query.
// Returns a *NotSingularError when more than one AnimalType ID is found.
// Returns a *NotFoundError when no entities are found.
func (atq *AnimalTypeQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = atq.Limit(2).IDs(setContextOp(ctx, atq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{animaltype.Label}
	default:
		err = &NotSingularError{animaltype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (atq *AnimalTypeQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := atq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AnimalTypes.
func (atq *AnimalTypeQuery) All(ctx context.Context) ([]*AnimalType, error) {
	ctx = setContextOp(ctx, atq.ctx, "All")
	if err := atq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AnimalType, *AnimalTypeQuery]()
	return withInterceptors[[]*AnimalType](ctx, atq, qr, atq.inters)
}

// AllX is like All, but panics if an error occurs.
func (atq *AnimalTypeQuery) AllX(ctx context.Context) []*AnimalType {
	nodes, err := atq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AnimalType IDs.
func (atq *AnimalTypeQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if atq.ctx.Unique == nil && atq.path != nil {
		atq.Unique(true)
	}
	ctx = setContextOp(ctx, atq.ctx, "IDs")
	if err = atq.Select(animaltype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (atq *AnimalTypeQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := atq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (atq *AnimalTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, atq.ctx, "Count")
	if err := atq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, atq, querierCount[*AnimalTypeQuery](), atq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (atq *AnimalTypeQuery) CountX(ctx context.Context) int {
	count, err := atq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (atq *AnimalTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, atq.ctx, "Exist")
	switch _, err := atq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (atq *AnimalTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := atq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AnimalTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (atq *AnimalTypeQuery) Clone() *AnimalTypeQuery {
	if atq == nil {
		return nil
	}
	return &AnimalTypeQuery{
		config:             atq.config,
		ctx:                atq.ctx.Clone(),
		order:              append([]OrderFunc{}, atq.order...),
		inters:             append([]Interceptor{}, atq.inters...),
		predicates:         append([]predicate.AnimalType{}, atq.predicates...),
		withAnimalTypeType: atq.withAnimalTypeType.Clone(),
		// clone intermediate query.
		sql:  atq.sql.Clone(),
		path: atq.path,
	}
}

// WithAnimalTypeType tells the query-builder to eager-load the nodes that are connected to
// the "animal_type_type" edge. The optional arguments are used to configure the query builder of the edge.
func (atq *AnimalTypeQuery) WithAnimalTypeType(opts ...func(*AnimalQuery)) *AnimalTypeQuery {
	query := (&AnimalClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	atq.withAnimalTypeType = query
	return atq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Type string `json:"type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AnimalType.Query().
//		GroupBy(animaltype.FieldType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (atq *AnimalTypeQuery) GroupBy(field string, fields ...string) *AnimalTypeGroupBy {
	atq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AnimalTypeGroupBy{build: atq}
	grbuild.flds = &atq.ctx.Fields
	grbuild.label = animaltype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Type string `json:"type,omitempty"`
//	}
//
//	client.AnimalType.Query().
//		Select(animaltype.FieldType).
//		Scan(ctx, &v)
func (atq *AnimalTypeQuery) Select(fields ...string) *AnimalTypeSelect {
	atq.ctx.Fields = append(atq.ctx.Fields, fields...)
	sbuild := &AnimalTypeSelect{AnimalTypeQuery: atq}
	sbuild.label = animaltype.Label
	sbuild.flds, sbuild.scan = &atq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AnimalTypeSelect configured with the given aggregations.
func (atq *AnimalTypeQuery) Aggregate(fns ...AggregateFunc) *AnimalTypeSelect {
	return atq.Select().Aggregate(fns...)
}

func (atq *AnimalTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range atq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, atq); err != nil {
				return err
			}
		}
	}
	for _, f := range atq.ctx.Fields {
		if !animaltype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if atq.path != nil {
		prev, err := atq.path(ctx)
		if err != nil {
			return err
		}
		atq.sql = prev
	}
	return nil
}

func (atq *AnimalTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AnimalType, error) {
	var (
		nodes       = []*AnimalType{}
		_spec       = atq.querySpec()
		loadedTypes = [1]bool{
			atq.withAnimalTypeType != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AnimalType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AnimalType{config: atq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, atq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := atq.withAnimalTypeType; query != nil {
		if err := atq.loadAnimalTypeType(ctx, query, nodes,
			func(n *AnimalType) { n.Edges.AnimalTypeType = []*Animal{} },
			func(n *AnimalType, e *Animal) { n.Edges.AnimalTypeType = append(n.Edges.AnimalTypeType, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (atq *AnimalTypeQuery) loadAnimalTypeType(ctx context.Context, query *AnimalQuery, nodes []*AnimalType, init func(*AnimalType), assign func(*AnimalType, *Animal)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uint64]*AnimalType)
	nids := make(map[uint64]map[*AnimalType]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(animaltype.AnimalTypeTypeTable)
		s.Join(joinT).On(s.C(animal.FieldID), joinT.C(animaltype.AnimalTypeTypePrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(animaltype.AnimalTypeTypePrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(animaltype.AnimalTypeTypePrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := uint64(values[0].(*sql.NullInt64).Int64)
				inValue := uint64(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*AnimalType]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Animal](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "animal_type_type" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (atq *AnimalTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := atq.querySpec()
	_spec.Node.Columns = atq.ctx.Fields
	if len(atq.ctx.Fields) > 0 {
		_spec.Unique = atq.ctx.Unique != nil && *atq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, atq.driver, _spec)
}

func (atq *AnimalTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(animaltype.Table, animaltype.Columns, sqlgraph.NewFieldSpec(animaltype.FieldID, field.TypeUint64))
	_spec.From = atq.sql
	if unique := atq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if atq.path != nil {
		_spec.Unique = true
	}
	if fields := atq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, animaltype.FieldID)
		for i := range fields {
			if fields[i] != animaltype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := atq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := atq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := atq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := atq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (atq *AnimalTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(atq.driver.Dialect())
	t1 := builder.Table(animaltype.Table)
	columns := atq.ctx.Fields
	if len(columns) == 0 {
		columns = animaltype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if atq.sql != nil {
		selector = atq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if atq.ctx.Unique != nil && *atq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range atq.predicates {
		p(selector)
	}
	for _, p := range atq.order {
		p(selector)
	}
	if offset := atq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := atq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AnimalTypeGroupBy is the group-by builder for AnimalType entities.
type AnimalTypeGroupBy struct {
	selector
	build *AnimalTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (atgb *AnimalTypeGroupBy) Aggregate(fns ...AggregateFunc) *AnimalTypeGroupBy {
	atgb.fns = append(atgb.fns, fns...)
	return atgb
}

// Scan applies the selector query and scans the result into the given value.
func (atgb *AnimalTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, atgb.build.ctx, "GroupBy")
	if err := atgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnimalTypeQuery, *AnimalTypeGroupBy](ctx, atgb.build, atgb, atgb.build.inters, v)
}

func (atgb *AnimalTypeGroupBy) sqlScan(ctx context.Context, root *AnimalTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(atgb.fns))
	for _, fn := range atgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*atgb.flds)+len(atgb.fns))
		for _, f := range *atgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*atgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := atgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AnimalTypeSelect is the builder for selecting fields of AnimalType entities.
type AnimalTypeSelect struct {
	*AnimalTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ats *AnimalTypeSelect) Aggregate(fns ...AggregateFunc) *AnimalTypeSelect {
	ats.fns = append(ats.fns, fns...)
	return ats
}

// Scan applies the selector query and scans the result into the given value.
func (ats *AnimalTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ats.ctx, "Select")
	if err := ats.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnimalTypeQuery, *AnimalTypeSelect](ctx, ats.AnimalTypeQuery, ats, ats.inters, v)
}

func (ats *AnimalTypeSelect) sqlScan(ctx context.Context, root *AnimalTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ats.fns))
	for _, fn := range ats.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
